KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Lingkaran" — index,
UnsurUnsurPage.tsx, KaitanBangunDatarPage.tsx, BusurJuringPage.tsx,
KelilingLuasPage.tsx, dan SudutPusatKelilingPage.tsx SUDAH
terverifikasi selesai dengan bukti kode konkret. Ini SUB-HALAMAN
TERAKHIR: PenerapanKontekstualPage.tsx (12 soal). PENTING: file ini
BELUM DIKERJAKAN SAMA SEKALI meskipun ada laporan lama yang salah
mengklaim sudah selesai (81 key, dll) — itu HALUSINASI dari sesi
sebelumnya, abaikan sepenuhnya, kerjakan file ini benar-benar dari
nol.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Tidak ada isDark/useTheme — hardcode dark-only
- File ini kemungkinan memakai <img> untuk soal (bukan CircleDiagram
  — cek dulu, jangan asumsi)
- WAJIB baca isi lengkap file INI SENDIRI dulu, lalu grep "\\text{"
  untuk cek kasus KaTeX bahasa Indonesia
- Setelah selesai, WAJIB tunjukkan BUKTI KONKRET (cuplikan kode
  aktual before/after dan isi locale key langsung dari file)

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: PenerapanKontekstualPage.tsx (12 soal,
tema soal cerita kontekstual: roda, kolam, lapangan, jam, pizza,
satelit, dll — pastikan terjemahan natural).

LANGKAH:
1. Baca isi lengkap file ini terlebih dahulu
2. Pindahkan array questions ke DALAM komponen:

   const { t } = useTranslation();
   const p = 'practice.lingkaran.penerapanKontekstual';
   const questions: Q[] = [
     Qf(1, t(`${p}.q1.title`), { ... }),
   ];

3. Terjemahkan SEMUA teks: title, content, parts[].text, alt text
   gambar (kalau ada <img>), natural sesuai konteks cerita
4. Grep "\\text{" — tangani SEMUA kasus bahasa Indonesia yang
   ditemukan (interpolasi kalau bercampur formula, konversi ke
   text: kalau kalimat prosa penuh — ikuti keputusan yang sama
   seperti di KelilingLuasPage)
5. Cek apakah ada reference box atau info box (pola yang sering
   ditemukan di halaman-halaman lain)
6. Terjemahkan h1 title dan badge jumlah soal
7. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.lingkaran.penerapanKontekstual.*
8. JANGAN sentuh sub-halaman lain atau halaman index

VERIFIKASI:
- Tunjukkan CUPLIKAN KODE AKTUAL (bukan cuma tabel) dari minimal 3
  soal sebelum dan sesudah perubahan
- Tunjukkan isi locale key yang benar-benar tertulis di id.json,
  en.json, ja.json (cuplikan langsung)
- Hitung dan tunjukkan JUMLAH t() call di dalam array questions
  (angka konkret)
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage)
- Konfirmasi eksplisit dengan bukti: SEMUA 12 soal sudah diterjemahkan
- KONFIRMASI PALING PENTING DAN TERAKHIR: setelah ini selesai, apakah
  SELURUH topik "Lingkaran" (index + 6 sub-halaman: 2+13+1+6+15+12 =
  49 soal) BENAR-BENAR 100% trilingual? Verifikasi dengan cek
  langsung ke semua 6 file (bukan cuma asumsi), tunjukkan bukti
  konkret untuk masing-masing