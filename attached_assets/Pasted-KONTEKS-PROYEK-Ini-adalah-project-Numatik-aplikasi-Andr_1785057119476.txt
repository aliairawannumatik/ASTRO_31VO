KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Pola Bilangan" —
index dan PengertianDanPolaKhususPage.tsx (10 soal) sudah selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- SVG <text> elements TIDAK diterjemahkan sama sekali — biarkan
  hardcode Indonesia
- JANGAN sentuh file orphan (PengertianPolaPage.tsx, PolaKhususPage.tsx)

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: PolaAritmetikaPage.tsx (15 soal).

KONTEKS KHUSUS FILE INI:
- Format data: array questions: QuestionItem[] via .map() (bukan JSX
  inline) — ikuti pola yang sudah terbukti di
  PengertianDanPolaKhususPage.tsx (title/content/textKey via t(),
  array tidak berisi teks hardcode, hanya referensi key)
- 2 SVG dipakai (SvgBarisanObjek di soal 1, SvgBatuBata di soal 11) —
  SEMUA teks di dalam SVG <text> JANGAN diterjemahkan, biarkan
  hardcode Indonesia
- BlockMath formula reference box di bagian atas halaman — cek apakah
  ada \text{} bahasa Indonesia di dalamnya (kalau pure math/rumus
  universal, JANGAN disentuh; kalau ada label bahasa Indonesia,
  tangani dengan pola interpolasi)

WAJIB — 7 kasus \text{} bahasa Indonesia di parts[].math (tangani
SEMUA dengan pola interpolasi variabel: hitung t() di JS dulu,
interpolasikan ke template literal, persis pola yang sudah terbukti
di topik Himpunan sebelumnya):

1. "\\text{Tuliskan rumus } U_n \\text{ dari barisan tersebut.}"
   → kalimat penuh dengan U_n di tengah — pisahkan jadi teks-pre +
     InlineMath(U_n) + teks-post, ATAU pindahkan seluruhnya ke
     parts[].text kalau strukturnya memungkinkan
2. "\\text{Hitung nilai } U_{20}."
3. "\\text{Hitung nilai } U_{25}."
4. "\\text{Hitung nilai } U_{40}."
5. "\\text{Tentukan nilai } U_{15}."
6. "\\text{Tentukan } U_{30} \\text{ dari barisan tersebut.}"
7. "\\text{Hitung } U_{25}."

Juga ada kata connective yang perlu ditangani (kalau muncul sebagai
\text{} terpisah, bukan bagian dari kalimat di atas):
- "\\text{dan}" dalam konteks seperti
  "U_3 = 15 \\quad \\text{dan} \\quad U_8 = 35" — interpolasi kata
  "dan"/"and"/"と" sebagai variabel locale terpisah (bisa jadi shared
  key seperti common.dan kalau memang dipakai berulang)

Untuk kasus kalimat penuh (nomor 1 di atas dan sejenisnya) — gunakan
pola introPre/introEnd (pecah di sekitar simbol matematika) seperti
yang sudah dipakai di topik-topik Kelas 7 sebelumnya, BUKAN
menerjemahkan seluruh string \text{} sekaligus (karena urutan kata
bisa beda antar bahasa, terutama Jepang).

1. Terjemahkan SEMUA teks: instruksi umum (kalau ada), teks 15 soal
   (content dan/atau parts[].text/math tergantung type), formula
   reference box (kalau ada teks natural di dalamnya)
2. Tangani SEMUA 7 kasus \text{} + kata connective "dan" dengan pola
   interpolasi yang tepat
3. Terjemahkan h1 title dan badge jumlah soal
4. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.polaBilangan.polaAritmetika.*
5. JANGAN sentuh sub-halaman lain, halaman index, atau file orphan

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q15)
- Konfirmasi + tunjukkan kode: bagaimana SETIAP dari 7 kasus \text{}
  (plus kata "dan") ditangani dengan pola interpolasi?
- Konfirmasi eksplisit: kedua SVG teksnya tidak disentuh, BlockMath
  formula box ditangani dengan benar (disentuh atau tidak, sesuai
  isinya), dan SEMUA 15 soal sudah diterjemahkan tanpa \text{} bahasa
  Indonesia tersisa di dalam KaTeX
- Kalau proses ini terasa berat/panjang, beri tahu di akhir supaya
  bisa dipecah jadi 2 tahap untuk sub-halaman berikutnya
  (PolaGeometriPage.tsx)