Halo, melanjutkan pengembangan Numatik.

FILE: KerucutPage.tsx (materi-matematika/kelas9/bangun-ruang-sisi-lengkung/, di .migration-backup/)

STATUS: Slide 1-8 sudah trilingual (struktur, konten, SVG interaktif, dan kuis UnsurSoalQuiz semua sudah language-aware). TypeScript 0 error di setiap tahap.

CATATAN VERIFIKASI: Screenshot tool di halaman ini konsisten timeout untuk slide-slide tertentu (limitasi environment yang sudah berulang dikonfirmasi). Verifikasi via pembacaan kode langsung diperbolehkan kalau screenshot gagal — laporkan jelas mana yang terverifikasi visual vs kode.

TEMUAN YANG BELUM DIPERBAIKI (JANGAN dikerjakan di tahap ini, hanya untuk konteks):
- Prefix badge (LUAS/VOLUME) hardcode di ExampleCard
- 2 versi tombol toggle beda teks ("Sembunyikan" vs "Sembunyikan Pembahasan")
- Slide baru "Contoh Soal Garis Pelukis" dari gpExamples belum ditambahkan
- 3 BlockMath di soal latihan masih pakai \text{selimut} (2x) dan \text{pasir}/r_{\text{pasir}}/t_{\text{pasir}}

KONVENSI PROJECT:
- Subscript KaTeX kata bahasa alami → huruf Latin: L_{\text{selimut}}→L_s, r/t_{\text{penuh}}→r_1/t_1, r/t/V_{\text{pasir}}→r_2/t_2/V_2
- Bug wajib dihindari: key di object A dipanggil dari object B di JSX → blank screen

TUGAS TAHAP 8 (HANYA INI):
1. Terjemahkan SLIDE 9 — Contoh Soal Luas Permukaan (luasExamples, baris ~1720): judul, subtitle, semua soal & langkah pembahasan ke id/en/ja. Sekalian fix subscript \text{selimut} yang muncul di soal-soal ini → L_s
2. Terjemahkan SLIDE 10 — Contoh Soal Volume (volExamples, baris ~1800): judul, subtitle, semua soal & langkah pembahasan ke id/en/ja. Sekalian fix subscript \text{pasir}/r_{\text{pasir}}/t_{\text{pasir}} → r_2/t_2/V_2 (dan \text{penuh} → r_1/t_1 kalau muncul)
3. Pastikan key direferensikan dari object yang BENAR
4. TypeScript check
5. Coba screenshot, kalau timeout verifikasi via kode dan laporkan jelas
6. Laporkan, BERHENTI, buat checkpoint

Kalau file terlalu panjang untuk sekali jalan, kerjakan bertahap dan laporkan progress.