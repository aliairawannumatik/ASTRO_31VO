KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Garis Singgung
Lingkaran" — index dan PengertianPage.tsx sudah selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- File ini punya useTheme() sudah ada (light-mode) — JANGAN sentuh
  logic isDark, hanya terjemahkan teks
- WAJIB tunjukkan BUKTI KONKRET (cuplikan kode before/after, isi
  locale key langsung dari file) di laporan akhir

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: MenghitungPanjangPage.tsx (7 soal, semua
punya SVG inline SvgSatu-SvgTujuh, bukan GSLDiagram).

KONTEKS KHUSUS:
- questions: Q[] saat ini di LUAR komponen (baris 187) — WAJIB
  dipindahkan ke DALAM komponen:

  const { t } = useTranslation();
  const p = 'practice.garisSinggungLingkaran.menghitungPanjang';
  const questions: Q[] = [
    Qn(1, t(`${p}.q1.title`), { ... }),
  ];

- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- 17 kasus \text{} terdeteksi di baris 194-256 — SEMUA di dalam
  parts[].math. Tangani dengan pola interpolasi (t() dulu di JS,
  interpolasi ke template literal) atau konversi ke text: kalau
  memang kalimat prosa penuh — ikuti keputusan yang sama seperti
  PengertianPage
- Q7 adalah soal cerita kontekstual (roda mesin) — pastikan
  terjemahan natural
- SvgSatu-SvgTujuh adalah SVG inline custom (bukan GSLDiagram) — cek
  apakah ada teks Indonesia hardcode di dalam <text> SVG-nya (kalau
  ada, ini celah baru yang perlu ditangani juga, tunjukkan bukti
  kalau ditemukan)

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text
3. Tangani SEMUA kasus \text{} yang ditemukan dari grep menyeluruh
   (verifikasi ulang 17 kasus, cek kalau ada tambahan)
4. Cek dan tangani teks Indonesia di dalam SVG SvgSatu-SvgTujuh kalau
   ada
5. Terjemahkan h1 title dan badge jumlah soal
6. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.garisSinggungLingkaran.menghitungPanjang.*
7. JANGAN sentuh sub-halaman lain, halaman index, atau GSLDiagram.tsx

VERIFIKASI:
- Tunjukkan CUPLIKAN KODE AKTUAL before/after untuk minimal 3 soal
- Tunjukkan isi locale key yang benar-benar tertulis di ketiga file
- Hitung dan tunjukkan JUMLAH t() call (angka konkret)
- Jalankan grep "\\text{" untuk verifikasi, tunjukkan hasil mentahnya
  (bandingkan dengan 17 dari investigasi awal)
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual)
- Konfirmasi eksplisit dengan bukti: SEMUA 7 soal sudah diterjemahkan,
  SVG SvgSatu-SvgTujuh sudah dicek untuk teks hardcode