KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "SPLDV" — hub/index
dan 6 sub-halaman lain (DefinisiSPLDVPage, MetodeGrafikPage,
MetodeSubstitusiPage, MetodeEliminasiPage, MetodeCampuranPage,
ModelSPLDVPage) sudah selesai. Ini SUB-HALAMAN TERAKHIR dari topik
ini: PenyelesaianMasalahPage.tsx (8 soal).

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Tidak ada isDark/useTheme — hardcode dark-only
- BlockMath, parts[].math adalah PURE KaTeX — JANGAN disentuh
- Badge (UN/ANBK/TKA/AKM) — TIDAK diterjemahkan
- Label universal (a./b./A./B./(1)/(2)) — JANGAN diterjemahkan;
  label "Langkah N:" sebagai part.label terpisah — WAJIB
  diterjemahkan (label DAN text-nya keduanya via t())
- File ini adalah file "latihan-mandiri" — JANGAN bingung dengan file
  serupa di folder "materi-matematika" (kalau ada), pastikan path
  yang disentuh benar-benar di bawah latihan-mandiri/kelas8/spldv/

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: PenyelesaianMasalahPage.tsx (8 soal, tema:
"Penyelesaian Masalah" — kemungkinan soal aplikasi/HOTS yang
menggabungkan semua metode sebelumnya).

KONTEKS KHUSUS:
- questions: Q[] mungkin sudah pakai fungsi factory Qf (cek dulu file
  aslinya) — WAJIB dipindahkan ke DALAM komponen:

  const { t } = useTranslation();
  const p = 'practice.spldv.penyelesaianMasalah';
  const questions: Q[] = [
    Qf(1, t(`${p}.q1.title`), { ... }),
  ];

- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- Karena topik ini kemungkinan soal cerita/aplikasi, pastikan
  terjemahan natural (bukan literal), ikuti standar kualitas yang
  sudah terbukti di ModelSPLDVPage.tsx
- Cek TELITI setiap soal untuk label "Langkah N:" atau sejenisnya
- parts[].math dan blockMath — PURE MATH, JANGAN disentuh
- Grade subtitle dan back button suffix — WAJIB diterjemahkan
- Cek ulang dengan grep "\\text{" untuk memastikan memang 0 kasus

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text, label
   khusus (kalau ada), grade subtitle, back button suffix
3. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.spldv.penyelesaianMasalah.*
4. JANGAN sentuh sub-halaman lain atau hub/index

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q8)
- Jalankan grep "\\text{" untuk konfirmasi
- Konfirmasi eksplisit: grade subtitle + back button suffix sudah
  diterjemahkan, SEMUA 8 soal sudah diterjemahkan, BlockMath/
  parts[].math tidak disentuh, questions array sudah dipindahkan ke
  dalam komponen
- KONFIRMASI PALING PENTING DAN TERAKHIR: setelah ini selesai, apakah
  SELURUH topik "SPLDV" (hub/index + 7 sub-halaman:
  8+11+8+8+8+10+8 = 61 soal) sekarang 100% trilingual tanpa ada yang
  terlewat?