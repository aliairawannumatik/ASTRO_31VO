KONTEKS PROJECT:
Ini project Numatik, aplikasi edukasi matematika untuk siswa SMP, 
menggunakan pnpm workspace. Source code aktif ada di artifacts/numatik/src/ 
(pastikan Anda mengedit file di sini, BUKAN di .migration-backup/). 
Sistem tema pakai useTheme() dari ThemeContext (isDark boolean), dengan 
tema dark/luar-angkasa dan 5 tema terang lainnya.

Sudah diperbaiki sebelumnya dengan pola conditional isDark yang konsisten 
di banyak file, termasuk 6 file latihan-mandiri Kelas 7 (baru saja). 
Buka salah satu file tersebut sebagai REFERENSI pola warna yang benar — 
jangan bikin skema warna baru.

CATATAN PENTING dari pengalaman file-file sebelumnya:
1. "Answer result boxes" / kotak soal-jawaban level Mudah/Sedang/Sulit 
   sering terlewat — cari SECARA EKSPLISIT dulu sebelum scan className 
   JSX biasa.
2. Teks bersarang di dalam <strong> tag dan body text sering terlewat — 
   lakukan scan ulang kedua.
3. Tailwind gradient className (bg-gradient-to-br) TIDAK reliable di 
   elemen <svg> — ganti ke inline style.
4. Fill "white" di dalam elemen <mask> SVG adalah semantik mask — 
   JANGAN diubah.
5. Kalau ada array data (misal "cards") dengan properti gradient warna 
   hardcoded, ikuti pola dari file Kelas 7 yang baru diperbaiki: pindahkan 
   array ke dalam komponen (kalau masih di luar), tambahkan useTheme(), 
   buat gradient conditional (dark tetap 900/*, light pakai 50/* tints).

TUGAS: Perbaiki SEMUA hardcoded background gelap di 4 file berikut 
di folder src/pages/latihan-mandiri/kelas8/ (sesuaikan path persis):
- persamaan-garis-lurus/ContextualIllustrations.tsx (42 baris — file 
  besar, kerjakan sistematis per section)
- relasi-fungsi/PengertianFungsiPage.tsx (6 baris)
- relasi-fungsi/PengertianRelasiPage.tsx (5 baris)
- relasi-fungsi/GrafikFungsiPage.tsx (4 baris)

ATURAN:
1. JANGAN ubah logika interaktif (kalkulasi, grafik fungsi/relasi) — 
   HANYA warna.
2. JANGAN sentuh file lain di luar 4 file ini.

Setelah selesai:
3. Verifikasi TypeScript check per-file (0 error) untuk semua 4 file.
4. Lakukan scan ulang untuk elemen bersarang.
5. Commit ke branch main (TIDAK PERLU push, saya push manual sendiri).

Laporkan hasilnya per file (ringkas).