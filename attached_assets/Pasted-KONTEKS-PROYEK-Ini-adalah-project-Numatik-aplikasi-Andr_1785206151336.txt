KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Persamaan Garis
Lurus" di Kelas 8 — index, MenentukanPGLPage.tsx, GrafikPGLPage.tsx,
GradienPage.tsx, dan Hubungan2GarisPage.tsx sudah selesai 100%
trilingual. Ini SUB-HALAMAN TERAKHIR: AplikasiKontekstualPage.tsx
(14 soal setelah bug fix renumbering sebelumnya — array questions
awalnya loncat dari Q2 ke Q4 karena Q3 hilang, sudah diperbaiki
dengan renumber Q4-Q15 menjadi Q3-Q14 di sesi lalu).

STATUS SAAT INI — SUDAH DIKERJAKAN SEBELUMNYA:
- Bug fix renumbering (Q1-Q14 berurutan) — SUDAH SELESAI
- questions array sudah dipindahkan ke DALAM komponen — SUDAH SELESAI
- Q1-Q7: title, content, parts semua sudah diterjemahkan via t(),
  termasuk restrukturisasi ~21 parts dari math: (berisi kalimat
  prosa Indonesia) menjadi text: + t() — SUDAH SELESAI
- Locale key practice.persamaanGarisLurus.aplikasiKontekstual.* untuk
  Q1-Q7 + page-level (h1, grade, badgeSuffix, backTo) — SUDAH ADA di
  ketiga locale file (id.json, en.json, ja.json)
- YANG BELUM: Q8-Q14 (7 soal) masih hardcode Bahasa Indonesia

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- ContextualIllustrations.tsx dan CoordPlane.tsx — JANGAN disentuh
  komponennya, hanya panggilan/props
- Tidak ada isDark/useTheme — hardcode dark-only

TUGAS SEKARANG:
Fokus HANYA ke Q8-Q14 (7 soal terakhir) di AplikasiKontekstualPage.tsx.

KONTEKS KHUSUS — 12 kasus \text{} yang perlu ditangani di Q8-Q14
(distribusi: Q8×2, Q9×3, Q11×3, Q12×2, Q13×2 — Q10 dan Q14
kemungkinan 0 kasus, tapi verifikasi ulang dengan grep):
- Kalau \text{} berisi kalimat prosa PENUH (seperti pola Q1-Q7 yang
  sudah dikerjakan) → konversi langsung math: menjadi text: + t(),
  TANPA perlu interpolasi
- Kalau \text{} bercampur dengan simbol matematika singkat (seperti
  pola "A(1,3) \text{ dan } B(4,9)" di GradienPage/Hubungan2GarisPage
  sebelumnya) → pakai pola interpolasi (hitung t() dulu di JS,
  interpolasikan ke template literal), manfaatkan shared key kalau
  teksnya berulang

Cek juga apakah ada parts lain di Q8-Q14 yang pakai math: untuk
kalimat prosa (bukan cuma yang sudah terdeteksi 12 kasus \text{} di
atas) — restrukturisasi semuanya ke text: + t() seperti pola Q1-Q7.

1. Terjemahkan SEMUA teks Q8-Q14: title, content, parts[].text (asli
   maupun hasil restrukturisasi dari math:)
2. Tangani SEMUA kasus \text{} yang ditemukan dari grep menyeluruh
3. Tambahkan translation key lanjutan ke ketiga file locale (id, en,
   ja) di bawah practice.persamaanGarisLurus.aplikasiKontekstual.*
   (Q8-Q14) — JANGAN duplikat atau timpa key Q1-Q7 yang sudah ada
4. JANGAN sentuh sub-halaman lain, halaman index,
   ContextualIllustrations.tsx, atau CoordPlane.tsx

VERIFIKASI:
- Laporkan validasi teknis (tsc/esbuild kalau tersedia, atau grep
  manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q8-Q14)
- Jalankan grep "\\text{" untuk verifikasi 0 hardcode tersisa di
  SELURUH file (Q1-Q14)
- Konfirmasi eksplisit: SEMUA 14 soal (Q1-Q14) sudah diterjemahkan,
  ContextualIllustrations dan CoordPlane tidak disentuh
- KONFIRMASI PALING PENTING DAN TERAKHIR: setelah ini selesai, apakah
  SELURUH topik "Persamaan Garis Lurus" (index + 5 sub-halaman:
  MenentukanPGL 10 + GrafikPGL 10 + Gradien 10 + Hubungan2Garis 8 +
  AplikasiKontekstual 14 = 52 soal) sekarang 100% trilingual tanpa
  ada yang terlewat?