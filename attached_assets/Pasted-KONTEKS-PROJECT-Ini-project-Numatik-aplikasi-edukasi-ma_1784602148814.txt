KONTEKS PROJECT:
Ini project Numatik, aplikasi edukasi matematika untuk siswa SMP, 
menggunakan pnpm workspace. Source code aktif ada di artifacts/numatik/src/ 
(pastikan Anda mengedit file di sini, BUKAN di .migration-backup/). 
Sistem tema pakai useTheme() dari ThemeContext (isDark boolean), dengan 
tema dark/luar-angkasa dan 5 tema terang lainnya.

Sebelumnya sudah diperbaiki PenerapanKontekstualPage.tsx (persamaan-kuadrat) 
dan RuangSampelPage.tsx (peluang) di latihan-mandiri Kelas 9 dengan pola 
conditional isDark yang konsisten. Buka salah satu file tersebut, atau 
KuartilPage.tsx/RataRataPage.tsx di statistika, sebagai REFERENSI pola 
warna yang benar — jangan bikin skema warna baru.

POLA PERBAIKAN YANG SUDAH TERBUKTI:
1. Untuk komponen dengan akses hook: useTheme() + isDark ? "..." : "...".
2. Untuk SVG di level MODULE (tidak bisa akses hook): fill="var(--card)" 
   untuk background, fill="var(--card-foreground)" untuk teks/stroke.
3. Arrow-function shorthand SVG component yang butuh isDark → konversi 
   ke block-body function.
4. "Answer result boxes" dan teks bersarang dalam <strong> tag sering 
   terlewat — cari SECARA EKSPLISIT dan scan ulang kedua.
5. Cek SEMUA className termasuk opacity rendah.
6. Tint ~0.03 yang invisible di kedua tema boleh dibiarkan — jelaskan 
   di laporan.

TUGAS: Perbaiki SEMUA hardcoded background gelap di 2 file berikut 
di folder src/pages/latihan-mandiri/kelas9/fungsi-kuadrat/ (sesuaikan 
path persis):
- KontekstualPage.tsx (10 baris)
- MenggambarGrafikPage.tsx (7 baris — kemungkinan berisi SVG grafik 
  parabola, pastikan grid/axis/kurva tetap kontras baik di kedua tema)

ATURAN:
1. JANGAN ubah logika interaktif (kalkulasi, rendering grafik) — 
   HANYA warna.
2. JANGAN sentuh file lain di luar 2 file ini.

Setelah selesai:
3. Verifikasi TypeScript check per-file (0 error) untuk kedua file.
4. Lakukan scan ulang untuk elemen bersarang dan SVG yang mungkin 
   terlewat.
5. Commit ke branch main (TIDAK PERLU push, saya push manual sendiri).

Laporkan hasilnya per file (ringkas).