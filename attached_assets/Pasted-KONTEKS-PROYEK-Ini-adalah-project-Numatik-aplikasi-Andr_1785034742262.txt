KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik-topik sebelumnya di
Kelas 7 sudah selesai. Topik "Segitiga dan Segiempat" — index dan
sub-halaman 1-4 (GarisBeratBagiTinggiPage.tsx,
KelilingSegitigaSegiempatPage.tsx, LuasSegitigaPage.tsx,
LuasSegiempatPage.tsx) sudah selesai. Ini SUB-HALAMAN TERAKHIR dari
topik ini: KelilingLuasBangunTakBeraturanPage.tsx.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Gambar diagram (SVG/img) TIDAK diterjemahkan
- Rumus KaTeX (InlineMath maupun BlockMath) dengan \text{} berisi
  bahasa natural WAJIB diganti — ikuti pola interpolasi variabel yang
  sudah terbukti di Q15 LuasSegiempatPage (hitung teks lewat t()
  dulu di JS, baru masukkan ke string math via template literal)

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman ke-5 (terakhir):
KelilingLuasBangunTakBeraturanPage.tsx (15 soal, 3 Bagian).

KONTEKS KHUSUS FILE INI:
- 3 Bagian: A (Keliling, Q1–5), B (Luas, Q6–10), C (Aplikasi, Q11–15)
- Format essay semua, komponen Section, Q dengan prop badge — badge
  kategori WAJIB diterjemahkan (ikuti pola LuasSegiempatPage)
- InlineMath sangat banyak di semua soal — pecah jadi segmen t() di
  sekitar tiap InlineMath
- Ada tip box dengan bullet list hardcode Indonesia (3 poin strategi)
  — WAJIB diterjemahkan
- SVG diagram untuk Q1–5 (L-shape, T-shape, rumah, plus, grid) —
  JANGAN diterjemahkan, hanya teks soal/instruksi di sekitarnya
- Q15 punya sub-pertanyaan a/b/c PLUS <div> teks denah hardcode —
  PERHATIAN KHUSUS: teks denah ini kemungkinan berisi banyak label/
  keterangan natural (misal nama ruangan, ukuran, arah) — semua wajib
  diterjemahkan, pecah jadi key terpisah per baris/label kalau perlu,
  jangan digabung jadi satu string panjang
- Cek juga apakah ada \text{} di BlockMath/InlineMath yang perlu
  dipecah dengan pola interpolasi variabel (seperti Q15 LuasSegiempat)

1. Terjemahkan SEMUA teks: instruksi per Bagian (A/B/C), teks 15 soal,
   badge kategori tiap soal, tip box (3 poin strategi), dan teks denah
   di Q15 (sub-pertanyaan a/b/c)
2. Terjemahkan h1 title dan badge jumlah soal
3. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.segitigaSegiempat.kelilingLuasBangunTakBeraturan.*
4. Setelah selesai, konfirmasi bahwa SELURUH topik "Segitiga dan
   Segiempat" (index + 5 sub-halaman, total 70 soal) sekarang 100%
   trilingual

VERIFIKASI:
- Ambil screenshot halaman ini dalam Bahasa Indonesia kalau alat
  screenshot tersedia
- Kalau tidak tersedia (limitasi sandbox pasca-import GitHub seperti
  sebelumnya), laporkan validasi teknis yang dilakukan (tsc, HMR log,
  curl) DAN berikan tabel rincian key per soal/bagian seperti pola
  laporan sebelumnya — jangan tandai task selesai tanpa rincian ini
- Konfirmasi eksplisit: SEMUA 15 soal (3 Bagian), badge kategori, tip
  box, dan teks denah Q15 sudah diterjemahkan dengan benar