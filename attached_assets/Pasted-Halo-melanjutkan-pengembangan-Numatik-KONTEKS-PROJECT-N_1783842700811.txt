Halo, melanjutkan pengembangan Numatik.

KONTEKS PROJECT:
Numatik adalah aplikasi pembelajaran matematika trilingual (id/en/ja) dengan beberapa menu: Buku Animasi Matematika, Latihan Mandiri, LKPD, Math Game Arena. Topik yang sama bisa ada di beberapa menu dengan file berbeda — pastikan kerja di file yang benar.

MENU: BUKU ANIMASI MATEMATIKA
TOPIK: BANGUN RUANG SISI LENGKUNG (BRSL) KELAS 9

STATUS:
✅ TabungPage.tsx — 100% SELESAI trilingual, terverifikasi via screenshot nyata (semua 9 slide: judul, isi, badge level, prefix soal, tombol toggle, navigasi)
⏳ FILE BERIKUTNYA: KerucutPage.tsx (materi-matematika/kelas9/bangun-ruang-sisi-lengkung/, 2.033 baris)

PELAJARAN DARI TabungPage — WAJIB diterapkan di file ini:
1. Struktur "sections" harus berupa FUNGSI yang menerima parameter language (getSections(language)), BUKAN module-level constant — kalau tidak, konten tidak akan switch bahasa sama sekali
2. Cek SEMUA elemen kecil yang mudah terlewat: prefix badge soal (seperti "SOAL"/"LUAS"/"VOLUME"), tombol toggle jawaban ("Lihat Pembahasan"/"Sembunyikan") — ini sering hardcode terpisah dari objek terjemahan utama
3. Setiap key translation WAJIB direferensikan dari object yang BENAR di JSX (bug lama: key ditambah di object A tapi dipanggil dari object B → blank screen)
4. Verifikasi WAJIB pakai screenshot visual NYATA (bukan asumsi dari source code) — kutip teks persis dari hasil screenshot, bukan dari apa yang "seharusnya" ada di kode

Konvensi:
- Subscript KaTeX kata bahasa alami → huruf Latin (contoh: V_{\text{kerucut}} → V_k), BUKAN diterjemahkan
- Notasi universal (seperti n \text{ faktor}) dikecualikan

TUGAS TAHAP 1 — AUDIT DULU, JANGAN TULIS KODE:
1. Baca KerucutPage.tsx penuh
2. Cek: apakah "sections" (atau struktur serupa) sudah berupa fungsi language-aware, atau masih module-level constant seperti TabungPage sebelum diperbaiki
3. Identifikasi: berapa banyak slide/section, berapa soal latihan, apakah ada \text{} violations (sebutkan baris)
4. Cek juga elemen kecil yang berpotensi terlewat: prefix badge, tombol toggle, label kecil lain yang mungkin hardcode terpisah
5. Laporkan hasil audit dalam tabel, BERHENTI dan buat checkpoint — jangan mulai menulis kode dulu

Istilah untuk Kerucut:
- "Kerucut" → "Cone" / 円錐
- "Garis Pelukis" → "Slant Height" / 母線
- "Jari-jari Alas" → "Base Radius" / 底面の半径
- "Titik Puncak" → "Apex" / 頂点