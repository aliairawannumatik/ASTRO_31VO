KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Persamaan Garis
Lurus" — index sudah selesai (practice.persamaanGarisLurus.* sudah
ada strukturnya untuk title, subtopics, dll).

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Tidak ada isDark/useTheme di halaman ini — hardcode dark-only
- CoordPlane (komponen reusable dari topik Koordinat Kartesius) —
  JANGAN disentuh komponennya, hanya panggilan/props

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: MenentukanPGLPage.tsx (10 soal).

KONTEKS KHUSUS:
- questions: Q[] saat ini di LUAR komponen (level modul) — WAJIB
  dipindahkan ke DALAM komponen (setelah useTranslation()):

  const { t } = useTranslation();
  const p = 'practice.persamaanGarisLurus.menentukanPgl';
  const questions: Q[] = [
    Q(1, t(`${p}.q1.title`), { ... }),
  ];

  (Kalau nama fungsi helper "Q" konflik dengan type Q, rename jadi
  Qf seperti solusi di topik SPLDV sebelumnya)
- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- Halaman ini sudah bersih dari \text{} bahasa Indonesia (parts pakai
  text: field untuk prose, math: hanya untuk ekspresi murni) — tetap
  WAJIB grep "\\text{" untuk memastikan
- Q3, Q4, Q7, Q8, Q9, Q10 pakai CoordPlane — JANGAN disentuh
  komponennya, koordinat di props tetap sebagai data numerik
- Q7-Q10 punya choices[] (pilihan ganda A/B/C/D) — teks di choices
  yang berupa prosa Indonesia WAJIB diterjemahkan; kalau choices
  berupa math murni (ekspresi/koordinat), JANGAN disentuh
- Label seperti "a."/"b."/"c."/"d."/"(1)"/"(2)"/"Petunjuk:" — yang
  universal (huruf/angka) JANGAN diterjemahkan; "Petunjuk:" (kalau
  ada di halaman ini) WAJIB diterjemahkan karena itu kata Indonesia

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text, choices
   (yang prosa), label "Petunjuk:" (kalau ada)
3. Terjemahkan h1 title dan badge jumlah soal
4. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.persamaanGarisLurus.menentukanPgl.*
5. JANGAN sentuh sub-halaman lain, halaman index, atau CoordPlane.tsx

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q10)
- Jalankan grep "\\text{" untuk konfirmasi 0 kasus
- Konfirmasi eksplisit: SEMUA 10 soal sudah diterjemahkan, CoordPlane
  tidak disentuh, choices pilihan ganda (Q7-Q10) sudah ditangani
  dengan benar (prosa diterjemahkan, math dibiarkan)