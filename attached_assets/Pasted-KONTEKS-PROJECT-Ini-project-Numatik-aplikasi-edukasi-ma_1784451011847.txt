KONTEKS PROJECT:
Ini project Numatik, aplikasi edukasi matematika untuk siswa SMP, 
menggunakan pnpm workspace. Source code aktif ada di artifacts/numatik/src/ 
(pastikan Anda mengedit file di sini, BUKAN di .migration-backup/). 
Sistem tema pakai useTheme() dari ThemeContext (isDark boolean), dengan 
tema dark/luar-angkasa dan 5 tema terang lainnya.

Sebelumnya sudah diperbaiki: "answer result boxes" dan className JSX 
umum di KubusPage.tsx sudah conditional isDark. Sekarang lanjutkan 
perbaikan KHUSUS elemen <svg> diagram 3D kubus.

CATATAN PENTING:
1. Tailwind gradient className (bg-gradient-to-br) TIDAK reliable di 
   elemen <svg> — ganti ke inline style: style={{ background: isDark 
   ? "rgba(10,18,35,0.90)" : "rgba(241,245,249,0.95)" }}
2. Fill warna SVG label yang kontras di dark tapi INVISIBLE di background 
   putih — ganti ke shade lebih pekat di tema terang (misal fill hex 
   terang → shade lebih pekat sesuai warna yang sama).
3. Fill "white" di dalam elemen <mask> SVG adalah semantik mask — 
   JANGAN diubah, biarkan tetap putih.
4. Halaman ini render diagram 3D kubus — perhatikan garis/sisi/wireframe/
   titik sudut, pastikan tetap terlihat jelas dan kontras baik di 
   kedua tema (jangan sampai garis putih/terang jadi nyaris invisible 
   di background terang, atau garis gelap nyaris invisible di background 
   gelap).

TUGAS: Di file KubusPage.tsx, perbaiki SEMUA fill/stroke/background 
hex gelap di dalam elemen <svg> (diagram 3D kubus, wireframe, jaring-jaring, 
titik sudut, label sisi/rusuk).

ATURAN:
1. JANGAN ubah logika interaktif (kalkulasi, animasi rotasi 3D jika ada) 
   — HANYA warna.
2. JANGAN sentuh file lain.

Setelah selesai:
3. Verifikasi brace balance, TypeScript check per-file (0 error), dan 
   render dev server bersih.
4. Scan final: pastikan tidak ada hardcode warna gelap tersisa di 
   SELURUH file KubusPage.tsx (baik JSX maupun SVG).
5. Commit ke branch main (TIDAK PERLU push, saya push manual sendiri).

Laporkan hasilnya, dan konfirmasi eksplisit "seluruh KubusPage.tsx 
sudah bersih, tidak ada hardcode tersisa".