KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Mulai topik baru di Kelas 8:
"Lingkaran" — hub/index + 6 sub-halaman aktif (UnsurUnsurPage.tsx 2
soal, KelilingLuasPage.tsx 13 soal, KaitanBangunDatarPage.tsx 1 soal
dengan 11 sub-bagian a-k, BusurJuringPage.tsx 6 soal,
SudutPusatKelilingPage.tsx 15 soal, PenerapanKontekstualPage.tsx 12
soal). Tidak ada file orphan. Ada komponen shared CircleDiagram.tsx
(sudah dikonfirmasi aman — semua label props universal/matematis,
TIDAK perlu penanganan seperti CoordPlane) — tidak perlu disentuh
untuk terjemahan.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah (kecuali perbaikan bug
  jumlah soal yang diminta khusus di bawah)
- practice.lingkaran.* belum ada sama sekali di ketiga locale file —
  ini fondasi baru

TUGAS SEKARANG:
Fokus HANYA ke halaman hub/index: LingkaranPage.tsx

BAGIAN 1 — PERBAIKI BUG BADGE TOTAL SOAL:
Badge di hub saat ini menampilkan "76 Soal Total", padahal jumlah
aktual dari 6 sub-halaman adalah 2+13+1+6+15+12 = 49. Perbaiki jadi
49.

BAGIAN 2 — TERJEMAHKAN INDEX:
1. Ubah array subtopics (hardcode label & desc di luar component)
   menjadi pola subtopicsConfig — derive label & desc di dalam
   komponen pakai t(), IDENTIK dengan pola yang dipakai di
   PersamaanGarisLurusPage.tsx / SPLDVPage.tsx (index topik-topik
   sebelumnya)
2. Terjemahkan h1 title index
3. Terjemahkan teks body enrichment note (footer), kalau ada
   (misal "Dilengkapi Diagram SVG Interaktif")
4. Tambahkan translation key untuk 6 label & deskripsi sub-topik + h1
   + enrichment note (kalau ada) ke ketiga file locale (id, en, ja)
   di bawah practice.lingkaran.*
5. Key global yang sudah ada (breadcrumb, suffixSoal, backToGrade8,
   dst) JANGAN diubah
6. JANGAN sentuh 6 sub-halaman aktif — itu untuk tahap berikutnya
7. JANGAN sentuh CircleDiagram.tsx

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Konfirmasi eksplisit: badge total soal sekarang 49 (bukan 76),
  semua label/desc subtopik + h1 + footer sudah trilingual