KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Topik "Persamaan Garis Lurus" hampir selesai, tapi ditemukan
kontradiksi soal Hubungan2GarisPage.tsx — sebelumnya diklaim semua
23 kasus \text{} sudah ditangani dengan shared key (danText,
sejajarText, tegakLurusText), tapi laporan lain bilang 8 kasus di
Q1-Q2 (kata "dan") masih hardcode.

TUGAS SEKARANG:
1. Buka Hubungan2GarisPage.tsx dan jalankan grep "\\text{" di
   SELURUH file
2. Tunjukkan HASIL GREP LENGKAP (semua baris yang match), supaya bisa
   dipastikan apakah kata "dan" di Q1/Q2 benar-benar masih hardcode
   \text{ dan } ATAU sudah pakai \text{${danText}} seperti yang
   diklaim sebelumnya
3. KALAU ternyata masih ada yang hardcode (regresi atau memang belum
   sempat dikerjakan), PERBAIKI dengan pola interpolasi yang sama
   seperti bagian lain di file ini (const danText = t(`${p}.danText`)
   sudah ada di file — kalau ada, tinggal reuse; kalau belum ada,
   deklarasikan)
4. KALAU ternyata SUDAH benar (laporan sebelumnya yang salah, bukan
   laporan barusan), jelaskan kenapa laporan barusan bisa keliru
   (misal salah baca file, atau membaca versi cache lama)

ATURAN PENTING:
- TypeScript harus 0 error baru
- JANGAN ubah bagian lain di file ini yang sudah benar
- JANGAN sentuh sub-halaman lain, halaman index, atau CoordPlane.tsx

VERIFIKASI:
- Grep "\\text{" FINAL — pastikan 0 hardcode Indonesia (semua harus
  berbentuk \text{${variabel}})
- Konfirmasi eksplisit: apakah SEMUA 23 kasus (atau berapapun total
  yang benar) sudah ditangani dengan interpolasi, tanpa kecuali
- Setelah ini, konfirmasi FINAL: apakah SELURUH topik "Persamaan
  Garis Lurus" (index + 5 sub-halaman, 52 soal: MenentukanPGL 10,
  GrafikPGL 10, Gradien 10, Hubungan2Garis 8, AplikasiKontekstual 14)
  sekarang BENAR-BENAR 100% trilingual tanpa ada \text{} hardcode
  yang tersisa di manapun?