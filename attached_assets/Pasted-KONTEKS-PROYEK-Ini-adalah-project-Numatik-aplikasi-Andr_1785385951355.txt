KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Garis Singgung
Lingkaran" — index, PengertianPage.tsx, MenghitungPanjangPage.tsx,
dan GSPLPage.tsx sudah selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- WASPADA SHADOWING: kalau ada loop parts.map((p, pi) => ...) dan
  locale prefix juga bernama const p, RENAME loop parameter jadi
  "part" (bukan p) — ini pernah jadi risiko di GSPLPage kemarin
- Tidak ada isDark/useTheme di file ini (dark-only, sesuai
  investigasi awal)
- WAJIB tunjukkan BUKTI KONKRET di laporan akhir

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: GSPDPage.tsx (4 soal — paling sedikit di
topik ini).

KONTEKS KHUSUS:
- questions: Q[] saat ini di LUAR komponen (baris 21) — WAJIB
  dipindahkan ke DALAM komponen:

  const { t } = useTranslation();
  const p = 'practice.garisSinggungLingkaran.gspd';
  const questions: Q[] = [
    Qn(1, t(`${p}.q1.title`), { ... }),
  ];

- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- 2 kasus \text{} terdeteksi di baris 37, 47 — tangani dengan pola
  interpolasi atau konversi ke text: sesuai konteksnya
- Q1, Q3, Q4 pakai GSLDiagram variant="gspd-two-circles" — cek label
  props-nya (kemungkinan sudah universal/matematis, tapi verifikasi)
- Cek juga apakah ada d_{\text{GSPD}} yang perlu ditangani seperti
  kasus GSPL kemarin (kemungkinan tetap dibiarkan karena subscript
  singkatan matematika)

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text
3. Tangani 2 kasus \text{} yang ditemukan (verifikasi ulang dengan
   grep menyeluruh)
4. Cek GSLDiagram variant="gspd-two-circles" untuk teks hardcode
5. Terjemahkan h1 title dan badge jumlah soal
6. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.garisSinggungLingkaran.gspd.*
7. JANGAN sentuh sub-halaman lain, halaman index, atau GSLDiagram.tsx

VERIFIKASI:
- Tunjukkan CUPLIKAN KODE AKTUAL before/after untuk semua 4 soal
  (karena cuma 4, tunjukkan semuanya)
- Tunjukkan isi locale key yang benar-benar tertulis di ketiga file
- Hitung dan tunjukkan JUMLAH t() call (angka konkret)
- Jalankan grep "\\text{" untuk verifikasi, tunjukkan hasil mentahnya
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual)
- Konfirmasi eksplisit dengan bukti: SEMUA 4 soal sudah diterjemahkan