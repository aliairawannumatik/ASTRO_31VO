KONTEKS PROJECT:
Ini project Numatik, aplikasi edukasi matematika untuk siswa SMP, 
menggunakan pnpm workspace. Source code aktif ada di artifacts/numatik/src/ 
(pastikan Anda mengedit file di sini, BUKAN di .migration-backup/). 
Sistem tema pakai useTheme() dari ThemeContext (isDark boolean), dengan 
tema dark/luar-angkasa dan 5 tema terang lainnya.

Sebelumnya sudah diperbaiki: "answer result boxes", objek data soal, 
dan seluruh className JSX umum (5 sub-komponen, 6 section, 5 slide, 
slide shell) di PrismaPage.tsx sudah conditional isDark. Sekarang 
lanjutkan perbaikan KHUSUS elemen <svg> diagram 3D prisma yang tersisa.

Sudah diperbaiki sebelumnya dengan pola konsisten untuk SVG diagram 3D 
di LimasPage.tsx, KubusPage.tsx, dan BalokPage.tsx. Buka BalokPage.tsx 
sebagai REFERENSI pola warna SVG yang benar — strukturnya paling mirip 
(bangun ruang dengan rusuk/sisi/titik sudut/diagonal).

CATATAN PENTING pola warna SVG dari file-file sebelumnya:
1. Tailwind gradient className (bg-gradient-to-br) TIDAK reliable di 
   elemen <svg> — ganti ke inline style: style={{ background: isDark 
   ? "rgba(10,18,35,0.90)" : "rgba(241,245,249,0.95)" }}
2. Fill warna SVG label kontras di dark tapi INVISIBLE di background 
   putih — ganti ke shade lebih pekat di tema terang.
3. Fill "white" di dalam elemen <mask> SVG adalah semantik mask — 
   JANGAN diubah.
4. Pola warna referensi (dari BalokPage.tsx/KubusPage.tsx):
   - Wireframe face fills: dark rgba(30,41,59,x) → light rgba(241,245,249,x)
   - Structural stroke lines: dark #334155/#475569 → light #94a3b8
   - Vertex circles: dark #e0f2fe/#e2e8f0 → light #0ea5e9/#64748b
   - Label text putih: dark #f8fafc/#ffffff → light #1e293b/#0f172a
5. Face fills dengan hue cerah (biru, merah, kuning untuk membedakan 
   sisi) BOLEH dibiarkan kalau sudah kontras baik di kedua tema.
6. Arrow-function SVG component perlu dikonversi ke block-body function 
   supaya bisa panggil useTheme() — ikuti pola RusukAnimSVG/SisiBalokSVG 
   dkk.

TUGAS: Di file src/pages/materi-matematika/kelas8/bangun-ruang-sisi-datar/PrismaPage.tsx, 
perbaiki SEMUA fill/stroke/background hex gelap yang tersisa di dalam 
elemen <svg> — diagram 3D prisma (rusuk, sisi, titik sudut, jaring-jaring, 
komponen animasi terkait seperti InteractivePrisma3D, RotatingPrisma3D, 
ThreePrismas kalau memuat elemen SVG di dalamnya).

ATURAN:
1. JANGAN ubah logika interaktif (kalkulasi, animasi rotasi) — HANYA warna.
2. JANGAN sentuh file lain.

Setelah selesai:
3. Verifikasi brace balance, TypeScript check per-file (0 error), dan 
   render dev server bersih.
4. Scan final menyeluruh: pastikan tidak ada hardcode warna gelap 
   tersisa di SELURUH file PrismaPage.tsx (JSX maupun SVG).
5. Commit ke branch main (TIDAK PERLU push, saya push manual sendiri).

Laporkan hasilnya, dan konfirmasi eksplisit "seluruh PrismaPage.tsx 
sudah bersih, tidak ada hardcode tersisa".