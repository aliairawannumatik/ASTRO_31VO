KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "SPLDV" — hub/index,
DefinisiSPLDVPage.tsx (8 soal), dan MetodeGrafikPage.tsx (11 soal)
sudah selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Tidak ada isDark/useTheme — hardcode dark-only
- BlockMath, parts[].math adalah PURE KaTeX — JANGAN disentuh
- Badge (UN/ANBK/TKA/AKM) — TIDAK diterjemahkan
- Label universal (a./b./A./B./(1)/(2)) — JANGAN diterjemahkan;
  label "Langkah N:" atau sejenisnya — WAJIB diterjemahkan (cek
  teliti, seperti yang ditemukan di Q3 MetodeGrafikPage kemarin)

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: MetodeSubstitusiPage.tsx (8 soal).

KONTEKS KHUSUS:
- questions: Q[] saat ini di LUAR komponen — WAJIB dipindahkan ke
  DALAM komponen, ikuti pola persis yang sudah terbukti:

  const { t } = useTranslation();
  const p = 'practice.spldv.metodeSubstitusi';
  const questions: Q[] = [
    Qf(1, t(`${p}.q1.title`), { ... }),
  ];

  (Rename fungsi factory dari Q ke Qf kalau ada konflik nama dengan
  type Q, seperti solusi di sub-halaman sebelumnya)
- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- Karena ini "Metode Substitusi", kemungkinan ada instruksi
  langkah-demi-langkah (misal "Langkah 1: Nyatakan salah satu
  variabel..."). Cek TELITI setiap soal untuk label seperti ini,
  jangan sampai terlewat
- parts[].math dan blockMath — PURE MATH, JANGAN disentuh
- Grade subtitle dan back button suffix — WAJIB diterjemahkan, ikuti
  pola grade/soalCount/backSuffix yang sudah terbukti
- Cek ulang dengan grep "\\text{" untuk memastikan memang 0 kasus

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text, label
   "Langkah N:" (kalau ada — cek teliti), grade subtitle, back button
   suffix
3. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.spldv.metodeSubstitusi.*
4. JANGAN sentuh sub-halaman lain atau hub/index

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q8)
- Jalankan grep "\\text{" untuk konfirmasi 0 kasus
- Konfirmasi eksplisit: grade subtitle + back button suffix sudah
  diterjemahkan, SEMUA 8 soal (termasuk label "Langkah N:" kalau ada)
  sudah diterjemahkan, BlockMath/parts[].math tidak disentuh,
  questions array sudah dipindahkan ke dalam komponen