KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "SPLDV" — hub/index
sudah trilingual, DefinisiSPLDVPage.tsx (8 soal) sudah selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Tidak ada isDark/useTheme — hardcode dark-only, JANGAN tambahkan
  conditional styling
- BlockMath, parts[].math adalah PURE KaTeX — 0 kasus \text{}
  terkonfirmasi di investigasi awal, JANGAN disentuh sama sekali
- Badge (UN/ANBK/TKA/AKM) — akronim internasional, TIDAK diterjemahkan
- Label universal seperti "a."/"b."/"A."/"B."/"(1)"/"(2)" — JANGAN
  diterjemahkan; hanya label seperti "Langkah N:" (kalau ada) yang
  perlu diterjemahkan

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: MetodeGrafikPage.tsx (11 soal).

KONTEKS KHUSUS:
- questions: Q[] saat ini di LUAR komponen — WAJIB dipindahkan ke
  DALAM komponen (setelah useTranslation()), ikuti pola persis yang
  sudah terbukti di DefinisiSPLDVPage.tsx:

  const { t } = useTranslation();
  const p = 'practice.spldv.metodeGrafik';
  const questions: Q[] = [
    Qf(1, t(`${p}.q1.title`), { ... }),
  ];

  (Kalau nama fungsi factory di file ini juga "Q" yang konflik dengan
  type Q, rename jadi Qf seperti solusi sebelumnya)
- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- parts[].label — kalau universal (a./b./A./B./dst), JANGAN
  diterjemahkan; kalau ada "Langkah N:" atau label bahasa Indonesia
  lain, WAJIB diterjemahkan
- parts[].math dan blockMath — PURE MATH, JANGAN disentuh
- Grade subtitle dan back button suffix — WAJIB diterjemahkan, ikuti
  pola grade/soalCount/backSuffix yang sudah terbukti di
  DefinisiSPLDVPage.tsx
- Karena halaman ini soal "Metode Grafik", cek apakah ada instruksi
  teks terkait menggambar grafik (misal "gambarkan pada bidang
  koordinat") — ini WAJIB diterjemahkan sebagai bagian dari
  content/parts.text
- Cek ulang dengan grep "\\text{" untuk memastikan memang 0 kasus

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text, label
   "Langkah N:" (kalau ada), grade subtitle, back button suffix
3. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.spldv.metodeGrafik.*
4. JANGAN sentuh sub-halaman lain atau hub/index

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q11)
- Jalankan grep "\\text{" untuk konfirmasi 0 kasus
- Konfirmasi eksplisit: grade subtitle + back button suffix sudah
  diterjemahkan, SEMUA 11 soal sudah diterjemahkan, BlockMath/
  parts[].math tidak disentuh, questions array sudah dipindahkan ke
  dalam komponen