KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Koordinat Kartesius"
— index sudah selesai (practice.koordinatCartesius.* sudah ada
strukturnya untuk title, subtopics, dll).

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- JANGAN sentuh file orphan (PosisiRelatifPage.tsx) atau
  CoordPlane.tsx (komponen SVG reusable, hanya menerima props numerik,
  tidak ada teks Indonesia hardcode di dalamnya untuk diterjemahkan)

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: UnsurUnsurPage.tsx (15 soal).

KONTEKS KHUSUS FILE INI:
- Format data: array questions: Q[] via .map(), dibangun dengan
  builder function Q()/Qn() — ikuti pola yang sudah terbukti di
  PolaAritmetikaPage.tsx / PolaGeometriPage.tsx (title/content/parts
  via t(), array tidak berisi teks hardcode, hanya referensi key)
- 8 dari 15 soal pakai diagram CoordPlane (Q1, Q2, Q4, Q8, Q9, Q10,
  Q11, Q14) — komponen ini TIDAK perlu diterjemahkan, koordinat titik
  sudah berupa data numerik di props, bukan teks Indonesia
- Koordinat titik dalam content/title (plain text, bukan InlineMath),
  misal "A(5, −3), B(−2, −4)" — ini pure notasi, TIDAK perlu
  diterjemahkan, tapi teks di SEKITARNYA (instruksi/kalimat) WAJIB
  diterjemahkan
- Koordinat/formula dalam parts[].math (InlineMath), misal
  "A(2,\ 4)" — pure math, JANGAN disentuh

WAJIB — 5 kasus \text{} bahasa Indonesia (tangani dengan pola yang
SAMA PERSIS seperti yang sudah terbukti di PolaAritmetikaPage/
PolaGeometriPage — danWord connector untuk kata "dan", textKey untuk
kalimat penuh yang mengapit simbol matematika):

1. "A(2,\ 4) \text{ dan } B(6,\ 8)" — danWord connector
2. "C(-3,\ 5) \text{ dan } D(7,\ -1)" — danWord connector
3. "E(0,\ 0) \text{ dan } F(-4,\ -6)" — danWord connector
4. "\text{Jika } P(x,\ y) \text{, rumus jarak ke sumbu-x adalah } ..."
   — kalimat penuh mengapit math → pindahkan ke textKey (JANGAN
   dipaksa jadi pre/post interpolasi karena strukturnya kompleks,
   ada koma dan multiple math di tengah)
5. "\text{Jika } P(x,\ y) \text{, rumus jarak ke sumbu-y adalah } ..."
   — sama, kalimat penuh → textKey

PENTING: cek ulang SELURUH file untuk kasus \text{} lain yang mungkin
belum terdeteksi di investigasi awal — investigasi topik sebelumnya
(Pola Bilangan) sempat melewatkan beberapa kasus, jadi pastikan grep
menyeluruh sebelum menganggap selesai.

1. Terjemahkan SEMUA teks: instruksi umum (kalau ada), teks 15 soal
   (title, content, parts[].text)
2. Tangani SEMUA kasus \text{} yang ditemukan (minimal 5, cek juga
   kalau ada tambahan) dengan pola danWord/textKey yang sesuai
3. Terjemahkan h1 title dan badge jumlah soal
4. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.koordinatCartesius.unsurUnsur.*
5. JANGAN sentuh sub-halaman lain, halaman index, file orphan, atau
   CoordPlane.tsx

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q15)
- Konfirmasi + tunjukkan kode: bagaimana SEMUA kasus \text{} ditangani?
- Jalankan grep "\\text{" untuk verifikasi 0 hardcode tersisa,
  laporkan hasil lengkapnya
- Konfirmasi eksplisit: SEMUA 15 soal sudah diterjemahkan, diagram
  CoordPlane tidak disentuh