Halo, melanjutkan pengembangan Numatik (baru saja pindah/migrasi ke akun Replit baru).

KONTEKS PROJECT:
Numatik adalah aplikasi pembelajaran matematika trilingual (id/en/ja) dengan beberapa menu: Buku Animasi Matematika, Latihan Mandiri, LKPD, Math Game Arena. Topik yang sama bisa ada di beberapa menu dengan file berbeda — pastikan kerja di file yang benar.

CATATAN PENTING SOAL STRUKTUR PROJECT:
Setiap kali project diimpor ke akun Replit baru, sistem Replit sering salah mendeteksi struktur project ini sebagai "pnpm workspace monorepo" dan otomatis memindahkan source code asli ke folder .migration-backup/, lalu membuat skeleton artifacts/ kosong. Ini BUKAN kesalahan kamu — ini pola berulang yang sudah dikenali. Di awal sesi ini:
1. Cek dulu apakah source ada di .migration-backup/ (bukan di lokasi aktif)
2. Kalau iya, daftarkan sebagai artifact resmi (buat artifact.toml, arahkan run command ke .migration-backup/, cek port routing Vite vs Express — solusi yang sudah terbukti di sesi sebelumnya: Vite hardcoded port 5000, Express baca $PORT, jadi set localPort=5000 dan PORT="3001" di artifact.toml agar tidak rebut port)
3. Pastikan proxy bisa routing dan halaman bisa diakses via browser preview
4. Setelah itu baru lanjut ke tugas utama di bawah

MENU: BUKU ANIMASI MATEMATIKA
TOPIK: BANGUN RUANG SISI LENGKUNG (BRSL) KELAS 9
FILE: materi-matematika/kelas9/bangun-ruang-sisi-lengkung/TabungPage.tsx (1.980 baris)

STATUS TERAKHIR (SUDAH DIVERIFIKASI dengan screenshot + git log — ini akurat, bukan asumsi):
- Hanya SLIDE 6 (Kesimpulan) yang benar-benar trilingual (id/en/ja lengkap)
- SLIDE 1-5 dan 7-9 (8 dari 9 slide) MASIH 100% hardcoded Bahasa Indonesia — judul, intro, SVG label, tabel, badge kesulitan, soal & jawaban, tombol navigasi, semuanya belum diterjemahkan
- Root cause teknis: variabel "sections" (baris ~873-1384) adalah MODULE-LEVEL CONSTANT, bukan fungsi yang menerima parameter "language" — makanya tidak bisa switch bahasa sama sekali
- Root cause proses: commit developer terakhir yang menyentuh file ini (51299a0, "refactored content and improved structure") adalah versi yang ada SEKARANG. Laporan sesi-sesi sebelumnya yang bilang "Tahap 2/3 selesai" TIDAK AKURAT — kemungkinan karena checkpoint sempat gagal ter-push ke GitHub sebelum pindah akun, jadi pekerjaan itu hilang.
- TypeScript: 0 error di file ini. Tidak ada console error.

KONVENSI PROJECT:
- Subscript KaTeX kata bahasa alami → huruf Latin (L_{\text{selimut}} → L_s), BUKAN diterjemahkan
- Bug wajib dihindari: key ditambah di object translations A tapi direferensikan dari object B di JSX → blank screen

TUGAS TAHAP A (HANYA INI, JANGAN LEBIH):
1. Refactor struktur "sections" dari module-level constant menjadi fungsi yang menerima `language` sebagai parameter, dipanggil di dalam komponen setelah useLanguage()
2. Terjemahkan SLIDE 1 (Pengertian Tabung, baris ~875-905) dan SLIDE 2 (Unsur-unsur Tabung, baris ~920-1151) ke id/en/ja — termasuk SVG label, tabel, dan tip kunci
3. Pastikan key translation direferensikan dari object yang BENAR di JSX
4. TypeScript check
5. Laporkan, BERHENTI, buat checkpoint

PENTING — SETELAH CHECKPOINT: Konfirmasi EKSPLISIT apakah checkpoint ini BERHASIL di-push ke GitHub (cek git log/git status, jangan cuma bilang "checkpoint dibuat" tanpa verifikasi). Ini WAJIB karena masalah checkpoint gagal ter-push adalah penyebab pekerjaan sebelumnya hilang.