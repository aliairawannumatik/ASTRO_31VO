KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Persamaan Garis
Lurus" — index, MenentukanPGLPage.tsx, dan GrafikPGLPage.tsx sudah
selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- Tidak ada isDark/useTheme — hardcode dark-only
- CoordPlane, GridLineChart — JANGAN disentuh komponennya, hanya
  panggilan/props
- WAJIB grep "\\text{" menyeluruh — JANGAN percaya angka investigasi
  awal begitu saja (di sub-halaman sebelumnya, angka sempat akurat
  di satu file dan meleset jauh di file lain)

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: GradienPage.tsx (10 soal).

KONTEKS KHUSUS:
- questions: Q[] saat ini di LUAR komponen — WAJIB dipindahkan ke
  DALAM komponen, ikuti pola yang sudah terbukti (rename Q ke Qf
  kalau perlu, hati-hati shadowing variabel "p" seperti yang
  ditemukan di GrafikPGLPage kemarin — pakai nama lain seperti "pt"
  kalau perlu loop variable):

  const { t } = useTranslation();
  const p = 'practice.persamaanGarisLurus.gradien';
  const questions: Q[] = [
    Qf(1, t(`${p}.q1.title`), { ... }),
  ];

- title, content, parts[].text — SEMUA WAJIB diterjemahkan via t()
- Kasus \text{} yang sudah terdeteksi di investigasi awal (perlu
  verifikasi ulang via grep, bisa jadi lebih/kurang dari ini):
  * Q5 parts a/b/c: "\text{Hitung } \Delta x...", "\text{Hitung }
    \Delta y...", "\text{Hitung gradien } m = ..."
  * Q8b: "\text{Hitung } m_1 \times m_2. \text{ Berapa hasilnya?}"
  * Q9/Q10 parts: "A(...) \text{ segaris. Tentukan nilai } k." dsb.
  Tangani SEMUA dengan pola interpolasi yang sudah terbukti (t() di
  JS dulu, interpolasi ke template literal)
- GridLineChart (SVG custom, kertas berpetak tanpa sumbu) dipakai di
  Q1 (multi-segment) dan Q5-Q8 — JANGAN disentuh komponennya
- CoordPlane dipakai di Q5-Q8 — JANGAN disentuh
- Manfaatkan shared keys (seperti pola tablePartA/B/C di
  GrafikPGLPage) kalau ada teks identik yang berulang di beberapa
  soal, untuk efisiensi

1. Pindahkan array questions ke dalam komponen
2. Terjemahkan SEMUA teks: title, content, parts[].text
3. Tangani SEMUA kasus \text{} yang ditemukan dari grep menyeluruh
   (minimal 6 dari investigasi awal, tapi verifikasi ulang)
4. Terjemahkan h1 title dan badge jumlah soal
5. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.persamaanGarisLurus.gradien.*
6. JANGAN sentuh sub-halaman lain, halaman index, CoordPlane.tsx,
   atau komponen GridLineChart

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q10)
- Jalankan grep "\\text{" untuk verifikasi 0 hardcode tersisa,
  laporkan JUMLAH TOTAL kasus yang ditemukan (bandingkan dengan
  investigasi awal ~6)
- Konfirmasi eksplisit: SEMUA 10 soal sudah diterjemahkan,
  CoordPlane/GridLineChart tidak disentuh