KONTEKS PROYEK:
Ini adalah project Numatik — aplikasi Android edukasi matematika untuk
siswa SMP (Kelas 7-9), dibangun dengan Vite + React + Capacitor +
TypeScript. Package ID: com.numatik.official. Juga di-deploy sebagai
web di numatik.app lewat Vercel.

KONTEKS SISTEM TRILINGUAL:
Aplikasi sedang migrasi ke sistem trilingual (Bahasa Indonesia,
English, Japanese) pakai react-i18next dengan useTranslation() dan
file locale (id.json, en.json, ja.json). Topik "Relasi dan Fungsi" —
index, PengertianRelasiPage.tsx, PengertianFungsiPage.tsx, dan
GrafikFungsiPage.tsx sudah selesai.

ATURAN PENTING:
- TypeScript harus 0 error baru
- Struktur, layout, logic lain JANGAN diubah
- BanyakFungsiPage.tsx TIDAK pakai isDark (styling hardcode dark-only)
  — jangan tambahkan useTheme() atau conditional className, biarkan
  seperti aslinya
- BlockMath dipakai di info box header — cek apakah pure math atau
  ada label bahasa Indonesia yang perlu diterjemahkan
- Hindari heredoc dengan karakter emoji langsung di bash — pakai
  Python script dengan encoding eksplisit UTF-8 atau str_replace

TUGAS SEKARANG:
Fokus HANYA ke sub-halaman: BanyakFungsiPage.tsx (6 soal, 0 diagram).

KONTEKS KHUSUS FILE INI:
ArrowDiagram di-import tapi TIDAK dipakai di questions[] — biarkan
import-nya seperti itu, jangan dihapus atau diubah.

WAJIB — 9 kasus \text{} bahasa Indonesia (tangani dengan pola textKey
yang sudah terbukti di topik-topik sebelumnya — SEMUA kasus ini
berbentuk "label = ..." jadi kemungkinan besar paling pas dikonversi
seluruhnya jadi textKey, bukan pre/post interpolasi, karena strukturnya
"[label bahasa Indonesia] = [hasil]"):

1. "\text{Banyak fungsi dari A ke B} = \ldots"
2. "\text{Banyak fungsi dari B ke A} = \ldots"
3. "\text{Banyak fungsi dari A ke B} = \ldots" (soal berbeda)
4. "\text{Banyak fungsi dari B ke A} = \ldots" (soal berbeda)
5. "\text{Banyak korespondensi satu-satu dari A ke B} = \ldots"
6. "\text{Banyak fungsi dari P ke Q} = \ldots"
7. "\text{Banyak fungsi dari Q ke P} = \ldots"
8. "\text{Banyak korespondensi satu-satu yang dapat dibuat} = \ldots"
9. "\text{Banyak cara menempati kamar} = \ldots \text{ (tulis jawaban
   dalam bentuk faktorial)}" — ini ada 2 \text{} dalam satu baris,
   perhatikan keduanya perlu ditangani

PENTING: karena ada beberapa label yang MIRIP tapi BEDA soal (misal
"Banyak fungsi dari A ke B" muncul di 2 soal berbeda), buat key
terpisah per soal (jangan di-reuse lintas soal) supaya lebih mudah
di-maintain, KECUALI kalau memang labelnya identik dan berulang
dalam soal yang sama.

Cek ulang SELURUH file dengan grep "\\text{" untuk memastikan tidak
ada kasus lain yang terlewat dari 9 yang sudah terdeteksi.

1. Terjemahkan SEMUA teks: instruksi umum (kalau ada), teks 6 soal
   (title, content, parts[].text), info box header (BlockMath +
   label di sekitarnya)
2. Tangani SEMUA 9 kasus \text{} dengan pola textKey
3. Terjemahkan h1 title dan badge jumlah soal
4. Tambahkan translation key ke ketiga file locale (id, en, ja) di
   bawah practice.relasiDanFungsi.banyakFungsi.*
5. JANGAN sentuh sub-halaman lain, halaman index, atau ArrowDiagram.tsx

VERIFIKASI:
- Laporkan validasi teknis (tsc kalau tidak timeout, atau esbuild/
  grep manual + skrip Node.js pengecek key coverage, HMR log)
- Berikan tabel rincian key per soal (Q1-Q6)
- Konfirmasi + tunjukkan kode: bagaimana SEMUA 9 kasus \text{}
  ditangani (termasuk kasus 9 yang punya 2 \text{} dalam satu baris)?
- Jalankan grep "\\text{" untuk verifikasi 0 hardcode tersisa
- Konfirmasi eksplisit: SEMUA 6 soal sudah diterjemahkan, ArrowDiagram
  import tidak dihapus, TIDAK ada isDark yang ditambahkan (sesuai
  struktur asli)