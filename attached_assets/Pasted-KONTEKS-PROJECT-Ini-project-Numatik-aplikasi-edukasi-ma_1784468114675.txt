KONTEKS PROJECT:
Ini project Numatik, aplikasi edukasi matematika untuk siswa SMP, 
menggunakan pnpm workspace. Source code aktif ada di artifacts/numatik/src/ 
(pastikan Anda mengedit file di sini, BUKAN di .migration-backup/). 
Sistem tema pakai useTheme() dari ThemeContext (isDark boolean), dengan 
tema dark/luar-angkasa dan 5 tema terang lainnya.

Sudah diperbaiki sebelumnya dengan pola conditional isDark yang konsisten 
di banyak topik materi (Persamaan Garis Lurus, Teorema Pythagoras, 
Lingkaran, seluruh Bangun Ruang Sisi Datar). Buka salah satu file yang 
sudah diperbaiki (misal file apapun di kelas8/bangun-ruang-sisi-datar/) 
sebagai REFERENSI pola warna yang benar — jangan bikin skema warna baru.

CATATAN PENTING dari pengalaman file-file sebelumnya:
1. "Answer result boxes" / kotak soal-jawaban level Mudah/Sedang/Sulit 
   (objek data seperti *Examples[0/1/2] atau easy/med/hard_props) paling 
   sering terlewat — cari SECARA EKSPLISIT dulu sebelum scan className 
   JSX biasa.
2. Teks bersarang di dalam <strong> tag dan body text di dalam kotak 
   jawaban sering terlewat — lakukan scan ulang kedua khusus untuk ini.
3. Tailwind gradient className (bg-gradient-to-br) TIDAK reliable di 
   elemen <svg> — ganti ke inline style.
4. Fill "white" di dalam elemen <mask> SVG adalah semantik mask — 
   JANGAN diubah.

TUGAS: Perbaiki SEMUA hardcoded background gelap di 6 file berikut 
di folder src/pages/latihan-mandiri/kelas7/ (sesuaikan path persis 
dengan struktur project):
- aljabar/PenjumlahanPenguranganAljabarPage.tsx (5 baris)
- aljabar/SubstitusiBilanganAljabarPage.tsx (4 baris)
- aljabar/PemangkatanAljabarPage.tsx (3 baris)
- segitiga-segiempat/GarisBeratBagiTinggiPage.tsx (3 baris)
- aljabar/PerkalianAljabarPage.tsx (2 baris)
- pecahan/PembulatanDesimalPage.tsx (1 baris)

Karena masing-masing file sangat kecil (1-5 baris), kerjakan semuanya 
dalam 1 sesi ini secara sistematis: answer boxes dulu → className umum 
→ SVG kalau ada → scan ulang.

ATURAN:
1. JANGAN ubah logika interaktif — HANYA warna.
2. JANGAN sentuh file lain di luar 6 file ini.

Setelah selesai:
3. Verifikasi TypeScript check per-file (0 error) untuk semua 6 file.
4. Lakukan scan ulang untuk elemen bersarang.
5. Commit ke branch main (TIDAK PERLU push, saya push manual sendiri).

Laporkan hasilnya per file (ringkas).