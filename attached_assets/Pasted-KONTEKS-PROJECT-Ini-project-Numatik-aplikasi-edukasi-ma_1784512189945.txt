KONTEKS PROJECT:
Ini project Numatik, aplikasi edukasi matematika untuk siswa SMP, 
menggunakan pnpm workspace. Source code aktif ada di artifacts/numatik/src/ 
(pastikan Anda mengedit file di sini, BUKAN di .migration-backup/). 
Sistem tema pakai useTheme() dari ThemeContext (isDark boolean), dengan 
tema dark/luar-angkasa dan 5 tema terang lainnya.

Sudah diperbaiki sebelumnya dengan pola conditional isDark yang konsisten 
di banyak file, termasuk GSLDiagram.tsx, GSPLPage.tsx, MasalahKontekstualPage.tsx 
di latihan-mandiri Kelas 8 (baru saja). Buka salah satu file tersebut 
sebagai REFERENSI pola warna yang benar — jangan bikin skema warna baru.

CATATAN PENTING:
1. Untuk komponen dengan akses hook: pakai useTheme() dan 
   isDark ? "..." : "...".
2. Untuk SVG di level MODULE (tidak bisa akses hook): pakai CSS custom 
   property — style={{ fill: "var(--card)" }} untuk background, 
   style={{ fill: "var(--card-foreground)" }} untuk teks/stroke.
3. Cek SEMUA className, termasuk yang tampak sepele seperti bg-white/5, 
   bg-black/X — ini juga perlu conditional, jangan dilewat meski 
   opacity-nya rendah.
4. "Answer result boxes" dan teks bersarang dalam <strong> tag sering 
   terlewat — scan ulang kedua wajib dilakukan.

TUGAS: Perbaiki SEMUA hardcoded background gelap di 7 file berikut 
di folder src/pages/latihan-mandiri/kelas8/ (sesuaikan path persis 
dengan struktur project):
- garis-singgung-lingkaran/MenghitungPanjangPage.tsx (3 baris)
- bangun-ruang-sisi-datar/BalokPage.tsx (5 baris)
- bangun-ruang-sisi-datar/KubusPage.tsx (5 baris)
- bangun-ruang-sisi-datar-gabungan/GabunganPage.tsx (1 baris)
- bangun-ruang-sisi-datar/LimasPage.tsx (1 baris)
- pola-bilangan/[3 file, masing-masing 1 baris — cari file yang relevan 
  di folder pola-bilangan/]

Karena semua file ini sangat kecil (1-5 baris masing-masing), kerjakan 
semuanya dalam 1 sesi ini secara sistematis satu per satu.

ATURAN:
1. JANGAN ubah logika interaktif — HANYA warna.
2. JANGAN sentuh file lain di luar yang disebutkan.

Setelah selesai:
3. Verifikasi TypeScript check per-file (0 error) untuk semua file.
4. Lakukan scan ulang untuk elemen bersarang.
5. Commit ke branch main (TIDAK PERLU push, saya push manual sendiri).

Laporkan hasilnya per file (ringkas).